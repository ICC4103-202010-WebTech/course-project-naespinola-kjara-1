<h2 class="text-info">❄ <%= @event.title%></h2>
<%@event_dates_to_votes.each do |event_dates_to_vote| %>

<div id="container" >
  <div class="row">
    <div  class="col-3">
      <aside id="profile_links">
        <ul class="table-success" id="links">
          <button type="button" class="list-group-item list-group-item-action p-2">image event</button>
          <button  type="button" class="list-group-item list-group-item-action p-2">Dates to event</button>
          <button type="button" class="list-group-item list-group-item-action p-2"><%=  event_dates_to_vote.date %></button>
<% end %>
          <button type="button" class="list-group-item list-group-item-action p-2">❄</button>
          <button type="button" class="list-group-item list-group-item-action p-2">Guests</button>
<% @event_guests.each do |event_guest| %>
          <button type="button" class="list-group-item list-group-item-action p-2"><%=event_guest.user.username%></button>
<% end %>

        </ul>
      </aside>
    </div>
<div class="col">
<table class="table table-striped" >
  <thead class="table-success">
  <th>
    📍 Location
  </th>
  </thead>
  <tbody>
  <tr>
    <td>
      <%= @event.location %>
    </td>
  </tr>
  </tbody>
</table>
    <table class="table table-striped" >
      <thead class="table-success">
      <th>
      Creation Date
      </th>
      </thead>
      <tbody>
      <tr>
        <td>
          <%= @event.created_at %>
        </td>
      </tr>
      </tbody>
    </table>
    <table class="table table-striped" >
      <thead class="table-success">
      <th>
        Description
      </thead>
      <tbody>
      <tr>
        <td>
          <%= @event.description%>
        </td>
      </tr>
      </tbody>
    </table>
    <table class="table table-striped" >
      <thead class="table-success">
      <th>
        Documents
      </thead>
      <tbody>
      <tr>
        <td>
          Videos <%= @event.videos%>
           .pdf <%= @event.documents%>
        </td>
      </tr>
      </tbody>
    </table>
    <div  class="table-success" >


    <div   class="font-weight-bolder">
      <p class="text-dark" >
       Wall Of Comments
      </p>

    </div>
    </div>

    <div class="list-group">
      <% @event_comments.each do |event_comment| %>
      <a href="#" class="list-group-item list-group-item-action">
        <div class="d-flex w-100 justify-content-between">
          <h5 class="mb-1"><%= event_comment.user.username%></h5>
          <small class="text-muted">🌙 Created at: <%= event_comment.created_at%></small>
        </div>
        <p class="mb-1"><%= event_comment.rich_text%></p>
        <small class="text-muted">Report</small>
        <small class="text-muted">Reply</small>

      <% end %>
      </a>
    </div>


  </div>
</div>

<br>
</div>
<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %> |
<%= link_to 'Delete',"dates_to_votes#destroy", method: :delete, data: { confirm: 'Are you sure?' } %>
