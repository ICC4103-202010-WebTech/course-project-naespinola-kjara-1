<div class="row ml-0 mt-3">
  <h2 class="text-info"> ✉️ Mailbox </h2>
</div>
<div class="row ml-0 mt-2">
  <p class="text-success">These are all your messages received!</p>
</div>
<div class="row ml-0 mt-2 mb-3">

</div>
<div class="row">
  <% @mailbox_message_receiver.each do |message| %>
    <%= render partial: 'mailbox/mailbox_tile', locals: { message: message} %>
  <% end %>
</div>



<%= link_to "New message", new_message_path, class: ["btn", "btn-info"] %>