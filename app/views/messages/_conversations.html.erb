<div>
  Chatting with <%= @messages.with(current_user).name %>
  <br>
  <em><%= message.personal_messages.last.body.truncate(50) %></em>
  <br>
  <%= link_to 'View conversation', message_path(message) %>
  <hr>
</div>