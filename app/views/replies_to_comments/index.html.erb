<p id="notice"><%= notice %></p>

<h1>Replies To Comments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @replies_to_comments.each do |replies_to_comment| %>
      <tr>
        <td><%= link_to 'Show', replies_to_comment %></td>
        <td><%= link_to 'Edit', edit_replies_to_comment_path(replies_to_comment) %></td>
        <td><%= link_to 'Destroy', replies_to_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Replies To Comment', new_replies_to_comment_path %>
